version: "2"

services:
  ereol:
    volumes:
      - 'ereol-webroot-sync:/var/www/html:nocopy'
      - './private/docker/sites/ereol:/var/www/html/sites/default:nocopy'
      - './private/docker/init/prepare-environment.sh:/etc/my_init.d/prepare-environment.sh:nocopy'
  ego:
    volumes:
      - 'ego-webroot-sync:/var/www/html:nocopy'
      - './private/docker/sites/ego:/var/www/html/sites/default:nocopy'
      - './private/docker/init/prepare-environment.sh:/etc/my_init.d/prepare-environment.sh:nocopy'
volumes:
  ereol-webroot-sync:
    external: true
  ego-webroot-sync:
    external: true
