diff --git a/README.md b/README.md
index 4cc92521..0de6404d 100644
--- a/README.md
+++ b/README.md
@@ -39,7 +39,6 @@ Handle translated roles https://www.drupal.org/node/1744274
 * Override material details - should be move into alters in base (feature_material_overrride.patch)
 * Update i18n module (i18n.patch)
 * Roll-back auto complete to open-suggestions (pensearch-autocomplete.patch)
-* OpenSearch cache id correction (opensearch-cache-cid.patch)
 * Removed the "Other formats" entity button (remove-other-formats-button.patch)
 * Remove auto-logout config (remove_autologout_configuration.patch)
 * Ensure infomedia field file is loaded (ting_infomedia-missing-include.patch)
diff --git a/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc b/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
index 5f134504..faa579f7 100644
--- a/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
+++ b/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
@@ -83,26 +83,6 @@ function ting_search_search_result_content_type_render($subtype, $conf, $panel_a
   }
 
   if (!empty($results['#results'])) {
-    // Set message.
-    $message = '';
-
-    // If it does not seem like the user picked a sort but our query has one,
-    // tell the user that we have sorted the result.
-    if (isset($search_result) && empty($conditions['sort']) && !empty($search_result->getSearchRequest()->getSorts())) {
-      $provider_sort_options = ting_search_sort_options();
-
-      $mapper = function(TingSearchSort $sort) use ($provider_sort_options) {
-        return isset($provider_sort_options[$sort->getField()]['label_short']) ? $provider_sort_options[$sort->getField()]['label_short'] : NULL;
-      };
-      $labels = array_filter(array_map($mapper, $search_result->getSearchRequest()->getSorts()));
-
-      $labels = implode(', ', $labels);
-      $msg = t('Records are sorted by %sortUsed . Select another sort if it is not suited', array('%sortUsed' => $labels));
-      $message .= '<div class="messages status">' . $msg . '</div>';
-    }
-
-    $prefix .= $message;
-
     $block->title = t('Search results');
     $block->content = $prefix . theme('ting_search_results', array('results' => $results['#results'], 'module' => $conf['type'])) . $suffix;
   }
diff --git a/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc b/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
index d7299b04..29e30860 100644
--- a/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
+++ b/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
@@ -28,19 +28,10 @@ function ting_search_result_count_content_type_render($subtype, $conf, $panel_ar
         '#theme' => 'html_tag',
         '#tag' => 'h1',
         '#attributes' => array('class' => array('page-title')),
-        '#value' => t('Search result'),
-      ),
-      'string' => array(
-        '#theme' => 'html_tag',
-        '#tag' => 'span',
-        '#attributes' => array('class' => array('search-string')),
-        '#value' => '"' . $search_result->getSearchRequest()->getFullTextQuery() . '"',
-      ),
-      'count' => array(
-        '#theme' => 'html_tag',
-        '#tag' => 'span',
-        '#attributes' => array('class' => array('count')),
-        '#value' => '(' . format_plural($results, '1 Result', '@count Results') . ')',
+        '#value' =>  t('Search result <span class="js-count" data-js-count="!count" class="search-field-in-content--amount">(!count !string)</span>', array(
+          '!count' => $results,
+          '!string' => format_plural($results > 1 ? $results : 1, 'Result', 'Results'),
+        ))
       ),
     );
   }
diff --git a/sites/all/patches/ting_search_result.patch b/sites/all/patches/ting_search_result.patch
index 3a38788d..e69de29b 100644
--- a/sites/all/patches/ting_search_result.patch
+++ b/sites/all/patches/ting_search_result.patch
@@ -1,77 +0,0 @@
-diff --git a/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc b/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
-index d08eb1c..5c48ea4 100644
---- a/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
-+++ b/profiles/ding2/modules/ting_search/plugins/content_types/search_result.inc
-@@ -91,25 +91,25 @@ function ting_search_search_result_content_type_render($subtype, $conf, $panel_a
- 
-     // If it does not seem like the user picked a sort but our query has one,
-     // tell the user that we have sorted the result.
--    if (isset($search_result) && empty($conditions['sort']) && !empty($search_result->getSearchRequest()->getSorts())) {
--      $provider_sort_options = ting_search_sort_options();
--
--      $mapper = function(TingSearchSort $sort) use ($provider_sort_options) {
--        return isset($provider_sort_options[$sort->getField()]['label_short']) ? $provider_sort_options[$sort->getField()]['label_short'] : NULL;
--      };
--      $labels = array_filter(array_map($mapper, $search_result->getSearchRequest()->getSorts()));
--
--      $labels = implode(', ', $labels);
--      $msg = t('Records are sorted by %sortUsed . Select another sort if it is not suited', array('%sortUsed' => $labels));
--      $message .= '<div class="messages status">' . $msg . '</div>';
--    }
--
--
--    if (isset($search_result) && $limit < $search_result->getNumTotalObjects()) {
--      $msg = t('Your search gave more than %s results. Try to search more specific or use the facets to filter the result.');
--      $msg = sprintf($msg, $limit);
--      $message .= '<div class="messages search-field-in-content--message">' . $msg . '</div>';
--    }
-+//    if (isset($search_result) && empty($conditions['sort']) && !empty($search_result->getSearchRequest()->getSorts())) {
-+//      $provider_sort_options = ting_search_sort_options();
-+//
-+//      $mapper = function(TingSearchSort $sort) use ($provider_sort_options) {
-+//        return isset($provider_sort_options[$sort->getField()]['label_short']) ? $provider_sort_options[$sort->getField()]['label_short'] : NULL;
-+//      };
-+//      $labels = array_filter(array_map($mapper, $search_result->getSearchRequest()->getSorts()));
-+//
-+//      $labels = implode(', ', $labels);
-+//      $msg = t('Records are sorted by %sortUsed . Select another sort if it is not suited', array('%sortUsed' => $labels));
-+//      $message .= '<div class="messages status">' . $msg . '</div>';
-+//    }
-+//
-+//
-+//    if (isset($search_result) && $limit < $search_result->getNumTotalObjects()) {
-+//      $msg = t('Your search gave more than %s results. Try to search more specific or use the facets to filter the result.');
-+//      $msg = sprintf($msg, $limit);
-+//      $message .= '<div class="messages search-field-in-content--message">' . $msg . '</div>';
-+//    }
- 
-     $prefix .= $message;
- 
-diff --git a/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc b/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
-index d7299b0..29e3086 100644
---- a/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
-+++ b/profiles/ding2/modules/ting_search/plugins/content_types/search_result_count.inc
-@@ -28,19 +28,10 @@ function ting_search_result_count_content_type_render($subtype, $conf, $panel_ar
-         '#theme' => 'html_tag',
-         '#tag' => 'h1',
-         '#attributes' => array('class' => array('page-title')),
--        '#value' => t('Search result'),
--      ),
--      'string' => array(
--        '#theme' => 'html_tag',
--        '#tag' => 'span',
--        '#attributes' => array('class' => array('search-string')),
--        '#value' => '"' . $search_result->getSearchRequest()->getFullTextQuery() . '"',
--      ),
--      'count' => array(
--        '#theme' => 'html_tag',
--        '#tag' => 'span',
--        '#attributes' => array('class' => array('count')),
--        '#value' => '(' . format_plural($results, '1 Result', '@count Results') . ')',
-+        '#value' =>  t('Search result <span class="js-count" data-js-count="!count" class="search-field-in-content--amount">(!count !string)</span>', array(
-+          '!count' => $results,
-+          '!string' => format_plural($results > 1 ? $results : 1, 'Result', 'Results'),
-+        ))
-       ),
-     );
-   }
