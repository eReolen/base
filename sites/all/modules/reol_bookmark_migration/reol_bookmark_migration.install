<?php
/**
 * @file
 * Installation of reol_bookmark_migration module.
 */

/**
 * Implements hook_schema().
 */
function reol_bookmark_migration_schema() {
  $schema['reol_bookmark_migration_migrated_user'] = array(
    'description' => 'Contains list of user uids that have been migrated.',
    'fields' => array(
      'uid' => array(
        'description' => 'The uid of the user that have been migrated.',
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'type' => array(
        'description' => 'Which type of site this refers to. 1 = ereolen, 2 = netlydbog',
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
    ),
    'foreign keys' => array(
      'user' => array(
        'table' => 'users',
        'columns' => array('uid' => 'uid'),
      ),
    ),
    'primary key' => array('uid', 'type'),
  );

  $schema['reol_bookmark_migration_migrated_isbn'] = array(
    'description' => 'Contains list of user isbns that have been migrated.',
    'fields' => array(
      'uid' => array(
        'description' => 'The uid of the user that have been migrated.',
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'isbn' => array(
        'description' => 'The ISBN that have been migrated.',
        'type' => 'int',
        'size' => 'big',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
      'type' => array(
        'description' => 'Which type of site this refers to. 1 = ereolen, 2 = netlydbog',
        'type' => 'int',
        'unsigned' => TRUE,
        'not null' => TRUE,
      ),
    ),
    'foreign keys' => array(
      'user' => array(
        'table' => 'users',
        'columns' => array('uid' => 'uid'),
      ),
    ),
    'primary key' => array('uid', 'isbn', 'type'),
  );

  return $schema;
}
